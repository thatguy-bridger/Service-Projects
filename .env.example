DATABASE_URL="postgresql://user:password@localhost:5432/service_projects"
DATABASE_URL_UNPOOLED="postgresql://user:password@localhost:5432/service_projects"
NEXTAUTH_SECRET="replace-me"
NEXTAUTH_URL="http://localhost:3000"

# Optional sign-in providers — omit any of these and that provider just
# doesn't appear on the sign-in page. Email + password always works with
# no config, since it's handled entirely in our own database.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
AZURE_AD_CLIENT_ID=""
AZURE_AD_CLIENT_SECRET=""
AZURE_AD_TENANT_ID=""
RESEND_API_KEY=""
EMAIL_FROM=""
