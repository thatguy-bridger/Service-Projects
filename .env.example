DATABASE_URL="postgresql://user:password@localhost:5432/service_projects"
DATABASE_URL_UNPOOLED="postgresql://user:password@localhost:5432/service_projects"
NEXTAUTH_SECRET="replace-me"
NEXTAUTH_URL="http://localhost:3000"

# Optional sign-in providers — omit any of these and that provider just
# doesn't appear on the sign-in page. Email + password always works with
# no config, since it's handled entirely in our own database.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
AZURE_AD_CLIENT_ID=""
AZURE_AD_CLIENT_SECRET=""
AZURE_AD_TENANT_ID=""
RESEND_API_KEY="re_dwqfgyBK_P1sTY7BPfjsHCyb2wwQr2ZVc"
EMAIL_FROM="bridger1174@gmail.com"

# apps/rounds signup address step (Google Places Autocomplete + map +
# reverse geocoding). NEXT_PUBLIC_ prefix required — read in the browser.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""

# apps/rounds — reserved for future territory-fill work (SPEC.md §9);
# not used by the current signup flow, which now uses Google Maps above.
UGRC_API_KEY=""
